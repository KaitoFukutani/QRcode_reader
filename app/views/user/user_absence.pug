extends ../layout

block content
  // Page Content
  #header-area
    include ../utils/user_header.pug

    p 欠席
    form(action='/user/add_absence', method='POST')
      textarea#absence_reason(name='reason' type='textarea' placeholder="欠席理由" onKeyUp="countLength(value, 'textlength2')")
      p#textlength2 0/100文字
      input#absence_date(name='date' type='date')
      button(type="button" onclick='AddAbsenceModal()') 登録